Put jquery slider BOUND TO SELECT because it gives both options (slider or select)

put actual rubrics in.

record team on submission

Spontaneous is still not set up.

do all things marked TODO

if time:
 - test javascript-less
 - test on iphone and a few androids

LATER:
TODO when assigning ids:
    ideally, the numerical order of the ids should match the
    alpha sorting so that 
    sorted(full_team_ids) == sorted(team_ids_numerical_portion_only)
